<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Add Diagnostic</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>

  <%- include('../layout') %>

  <main class="container">
    <section class="tool-card">

      <h1>Add Diagnostic</h1>
      <p class="subtitle">
        For tool: <strong><%= tool.name %></strong>
      </p>

      <form action="/diagnostics/<%= tool._id %>" method="POST" class="form-stack">

        <label for="attackType">Attack Type</label>
        <input type="text" id="attackType" name="attackType" required />

        <label for="severity">Severity</label>
        <select id="severity" name="severity">
          <option>Low</option>
          <option>Medium</option>
          <option>High</option>
          <option>Critical</option>
        </select>

        <label for="note">Notes</label>
        <textarea id="note" name="note" rows="4"></textarea>

        <label class="checkbox-row">
          <input type="checkbox" name="resolved" />
          <span>Mark as Resolved</span>
        </label>

        <div class="form-actions">
          <button type="submit" class="btn-primary">Add Diagnostic</button>
          <a href="/tools/<%= tool._id %>" class="btn-ghost">Cancel</a>
        </div>

      </form>

    </section>
  </main>

</body>
</html>
