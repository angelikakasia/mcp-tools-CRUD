<nav class="navbar">
  <div class="nav-left">
    <a href="/tools" class="nav-logo">My Tools</a>

    <% if (user) { %>
      <a href="/tools/new">Add Tool</a>
    <% } %>
  </div>

  <div class="nav-right">
    <% if (user) { %>
      <span class="nav-user">Hello, <%= user.username %></span>
      <a href="/auth/logout" class="btn-link">Logout</a>
    <% } else { %>
      <a href="/auth/login" class="btn-link">Login</a>
      <a href="/auth/signup" class="btn-primary">Signup</a>
    <% } %>
  </div>
</nav>
